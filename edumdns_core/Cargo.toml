[package]
name = "edumdns_core"
version = "0.1.0"
edition = "2024"

[dependencies]
pcap = {  version = "2.2.0", features = ["tokio", "lending-iter", "futures"]}
pnet = "0.35.0"
thiserror = "2.0.12"
dns-parser = "0.8.0"

